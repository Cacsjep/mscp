Smart Bar
  1. Presets — Save current camera layout as a named preset, restore it later (like browser bookmark groups)
  2. Recent cameras — Show recently used cameras at the top for quick access
  3. Fuzzy search — Match "ax p32" to "Axis P3245" instead of requiring exact substring
  4. Export snapshots — Quick snapshot from a camera directly from the launcher
  5. Quick timeline jump — "Go to 14:30" or "Go to yesterday 20:00" to jump playback timefrom 