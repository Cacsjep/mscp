<UserControl x:Class="TimelineJump.Client.TimelineJumpSettingsPanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="600">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="Playback Behavior"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Margin="0,0,0,10" />
            <Border BorderBrush="#555"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="15"
                    Margin="0,0,0,10">
                <StackPanel>
                    <CheckBox x:Name="JumpToCurrentCheck"
                              Content="Jump to current date and time when entering Playback" />
                    <TextBlock Text="When enabled, the master timeline jumps to the current wall-clock time each time the operator switches into the Playback workspace, including when the Smart Client starts directly in Playback. Tiles in independent playback are left alone."
                               TextWrapping="Wrap"
                               Opacity="0.55"
                               FontSize="11"
                               Margin="22,6,0,0" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
