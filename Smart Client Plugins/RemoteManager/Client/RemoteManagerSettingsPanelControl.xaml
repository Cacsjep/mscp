<UserControl x:Class="RemoteManager.Client.RemoteManagerSettingsPanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400"
             d:DesignWidth="600">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">

            <TextBlock Text="Security"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Margin="0,0,0,10" />

            <Border BorderBrush="#555"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="15"
                    Margin="0,0,0,20">
                <StackPanel>
                    <CheckBox x:Name="HideCredentialBarCheck"
                              Content="Hide credentials toolbar"
                              FontSize="13"
                              Margin="0,0,0,6" />
                    <TextBlock Text="When enabled, the toolbar that shows hardware usernames and passwords (with Copy and Show buttons) is hidden inside the Remote Manager workspace. Recommended for installations where operators must not see or copy device credentials."
                               TextWrapping="Wrap"
                               Opacity="0.75"
                               FontSize="12"
                               Margin="24,0,0,0" />
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
